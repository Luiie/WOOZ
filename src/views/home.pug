doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title WOOZ
        link(rel="stylesheet", href="https://unpkg.com/mvp.css@1.12/mvp.css")
    body 
        header
            h1 WOOZ
        main
            div#welcome
                form
                    input(type="text", placehoder="Room Name", required)
                    button Enter the Room
                h4 Open Rooms:
                ul 
            div#room
                h3
                h5 Nickname: Anonymous
                form#name
                    input(type="text", placehoder="Nickname", required)
                    button Save
                ul
                form#msg
                    input(type="text", placehoder="Message", required)
                    button Send
        script(src="/socket.io/socket.io.js")
        script(src="/public/js/app.js")
        WebSocket